# Spatial Fabric SDK UI 使用说明

## 🌐 Gradio界面

### 特性
- **公网链接**: 自动生成公网链接，支持远程访问
- **现代化设计**: 使用Gradio的现代化UI组件
- **响应式布局**: 自适应不同屏幕尺寸
- **实时交互**: 所有操作都有实时反馈

### 启动方式

#### 方式1：使用Python函数
```python
import spatial_fabric_sdk as sf

# 启动Gradio界面
sf.gradio_ui()
```

#### 方式2：直接运行脚本
```bash
python run_gradio_ui.py
```

#### 方式3：运行模块脚本
```bash
python spatial_fabric_sdk/launch_gradio_ui.py
```

### 界面功能

#### 1. 系统配置
- **Gard服务地址**: 输入Gard服务的基础URL
- **Handle前缀**: 输入Handle系统的前缀
- **初始化客户端**: 点击按钮初始化SDK客户端

#### 2. Handle管理
- **注册Handle**: 为空间数据创建唯一标识符
- **解析Handle**: 根据Handle ID获取数据信息
- **搜索Handle**: 按条件搜索已注册的Handle

#### 3. 空间数据管理
- **创建数据**: 创建新的空间数据记录
- **读取数据**: 根据ID获取空间数据
- **更新数据**: 更新现有空间数据
- **删除数据**: 删除空间数据记录

#### 4. 搜索功能
- **标签搜索**: 按标签搜索空间数据
- **高级搜索**: 支持多条件组合搜索

#### 5. 数据查看
- 查看系统中的所有数据（开发中）

### 操作流程

1. **启动界面**: 运行启动命令
2. **配置服务**: 在配置区域输入服务信息
3. **初始化客户端**: 点击初始化按钮
4. **选择功能**: 通过标签页选择功能
5. **填写信息**: 在表单中填写必要信息
6. **执行操作**: 点击按钮执行操作
7. **查看结果**: 在界面上查看操作结果

### 公网访问

Gradio界面启动后会自动生成公网链接，格式如下：
```
Running on local URL:  http://0.0.0.0:7860
Running on public URL: https://xxxxx.gradio.live
```

- **本地访问**: 使用 `http://localhost:7860`
- **公网访问**: 使用生成的公网链接，如 `https://xxxxx.gradio.live`

## 🔧 安装依赖

### Gradio界面依赖
```bash
pip install gradio>=4.0.0 pandas>=1.5.0
```

### 完整依赖
```bash
pip install -r requirements.txt
```

## 🚀 快速开始

1. **安装依赖**:
   ```bash
   pip install gradio pandas
   pip install -e .
   ```

2. **启动Gradio界面**:
   ```bash
   python run_gradio_ui.py
   ```

3. **配置服务**:
   - 输入Gard服务地址
   - 输入Handle前缀
   - 点击"初始化客户端"

4. **开始使用**:
   - 选择功能标签页
   - 填写必要信息
   - 执行操作

## 💡 使用技巧

### Gradio界面
- **公网链接**: 界面启动后会自动显示公网链接
- **响应式设计**: 界面会自动适应不同屏幕尺寸
- **实时反馈**: 所有操作都有即时的结果反馈
- **JSON输出**: 操作结果以JSON格式显示，便于查看

## 🔍 故障排除

### 常见问题

1. **导入错误**
   ```
   解决方案: pip install -e .
   ```

2. **依赖缺失**
   ```
   解决方案: pip install gradio pandas
   ```

3. **端口占用**
   ```
   解决方案: 修改启动脚本中的端口号
   ```

4. **公网链接无法访问**
   ```
   解决方案: 检查网络连接，确保防火墙允许访问
   ```

### 日志查看

启动时会显示详细的日志信息，包括：
- 服务启动状态
- 公网链接生成
- 错误信息

## 📞 技术支持

如遇到问题，请：
1. 查看控制台错误信息
2. 检查依赖是否正确安装
3. 确认服务配置是否正确
4. 提交GitHub Issue

---

**Gradio界面**提供了优秀的用户体验和公网访问功能！ 